--- a/usr/share/perl5/Lemonldap/NG/Portal/Auth/OpenIDConnect.pm
+++ b/usr/share/perl5/Lemonldap/NG/Portal/Auth/OpenIDConnect.pm
@@ -13,7 +13,7 @@ use Lemonldap::NG::Portal::Main::Constants qw(
   PE_REDIRECT
 );
 
-our $VERSION = '2.22.0';
+our $VERSION = '2.23.0';
 
 extends qw(
   Lemonldap::NG::Portal::Main::Auth
@@ -141,12 +141,35 @@ sub extractFormInfo {
         $self->logger->debug(
             'OpenIDConnect callback URI detected: ' . $req->uri );
 
-        # AuthN Response
-        my $state = $req->param('state');
+        # Initialize callback_params with all known OIDC callback parameters
+        my $callback_params = {
+            state             => scalar( $req->param('state') ),
+            code              => scalar( $req->param('code') ),
+            error             => scalar( $req->param('error') ),
+            error_description => scalar( $req->param('error_description') ),
+            error_uri         => scalar( $req->param('error_uri') ),
+            iss               => scalar( $req->param('iss') ),
+        };
+
+        # Call hook - plugins can modify callback_params
+        my $hook_res =
+          $self->p->processHook( $req, 'oidcGotAuthenticationResponse',
+            $callback_params );
+        return $hook_res if ( $hook_res != PE_OK and $hook_res > 0 );
 
         # Restore state
+        my $state = $callback_params->{state};
         if ($state) {
-            if ( $self->extractState( $req, $state ) ) {
+
+          # If state is a hashref, hook already extracted it - skip extractState
+          # If state is a scalar, call extractState
+            if ( ref($state) eq 'HASH' ) {
+                $self->logger->debug("State already extracted by hook");
+                $req->data->{_oidcOPCurrent} = $state->{_oidcOPCurrent};
+                $req->data->{_oidcNonce}     = $state->{_oidcNonce};
+                $req->data->{_url}           = $state->{urldc};
+            }
+            elsif ( $self->extractState( $req, $state ) ) {
                 $self->logger->debug("State $state extracted");
             }
             else {
@@ -165,11 +188,33 @@ sub extractFormInfo {
 
         $self->logger->debug("Using OpenIDConnect Provider $op");
 
+        # RFC 9207: Verify iss parameter if present or required
+        my $iss          = $callback_params->{iss};
+        my $expected_iss = $self->opMetadata->{$op}->{conf}->{issuer};
+
+        if ($iss) {
+            unless ( $iss eq $expected_iss ) {
+                $self->userLogger->error(
+                    "Issuer mismatch: received '$iss', expected '$expected_iss'"
+                );
+                return PE_OIDC_AUTH_ERROR;
+            }
+            $self->logger->debug("Issuer verification passed");
+        }
+        elsif ( $self->conf->{oidcOPMetaDataOptions}->{$op}
+            ->{oidcOPMetaDataOptionsRequireIss} )
+        {
+            $self->userLogger->error(
+"Missing iss parameter in authorization response (required by config)"
+            );
+            return PE_OIDC_AUTH_ERROR;
+        }
+
         # Check error
-        my $error = $req->param("error");
+        my $error = $callback_params->{error};
         if ($error) {
-            my $error_description = $req->param("error_description");
-            my $error_uri         = $req->param("error_uri");
+            my $error_description = $callback_params->{error_description};
+            my $error_uri         = $callback_params->{error_uri};
 
             $self->logger->error("Error returned by $op Provider: $error");
             $self->logger->error("Error description: $error_description")
@@ -180,7 +225,7 @@ sub extractFormInfo {
         }
 
         # Get access_token and id_token
-        my $code = $req->param("code");
+        my $code = $callback_params->{code};
 
         my $content =
           $self->getAuthorizationCodeAccessToken( $req, $op, $code );
@@ -202,6 +247,21 @@ sub extractFormInfo {
             $self->logger->debug("Token response is valid");
         }
 
+        # Check if returned scopes match requested scopes (#3585)
+        # Ignore offline_access (often stripped by OP) and ordering differences
+        my $reqs = $self->opOptions->{$op}->{oidcOPMetaDataOptionsScope};
+        if ( my $recs = $token_response->{scope} ) {
+            my %scopes;
+            $scopes{$_} += 1 for split( /\s+/, $reqs );
+            $scopes{$_} -= 1 for split( /\s+/, $recs );
+            delete $scopes{offline_access};
+            if ( grep { $_ } values %scopes ) {
+                $self->userLogger->warn( "OP $op returned different scopes"
+                      . " than requested: requested='$reqs',"
+                      . " received='$recs'" );
+            }
+        }
+
         my $access_token  = $token_response->{access_token};
         my $expires_in    = $token_response->{expires_in};
         my $id_token      = $self->decryptJwt( $token_response->{id_token} );
@@ -522,16 +582,10 @@ sub frontLogout {
     my $badRequest = 0;
     my $logName    = 'OIDC FC Logout';
 
-    my $img      = $self->conf->{staticPrefix} . '/common/icons/ok.png';
-    my $frame    = qq'<html><body><img src="$img"></body></html>';
-    my $response = [
-        200,
-        [
-            'Content-Type'   => 'text/html',
-            'Content-Length' => length($frame)
-        ],
-        [$frame]
-    ];
+    my $img   = $self->conf->{staticPrefix} . '/common/icons/ok.png';
+    my $frame = qq'<html><body><img src="$img"></body></html>';
+    my $response =
+      $self->p->sendTextResponse( $req, $frame, type => 'text/html' );
 
     # LLNG requires sid and iss
     unless ( $iss and $sid ) {
@@ -721,7 +775,7 @@ sub backLogout {
         $self->logger->warn("$logName: no session found for $uid");
     }
 
-    return [ 200, [], [] ];
+    return $self->p->sendBinaryResponse( $req, '' );
 }
 
 1;
--- a/usr/share/perl5/Lemonldap/NG/Portal/Issuer/OpenIDConnect.pm
+++ b/usr/share/perl5/Lemonldap/NG/Portal/Issuer/OpenIDConnect.pm
@@ -25,7 +25,7 @@ use Lemonldap::NG::Portal::Main::Constants qw(
 );
 use String::Random qw/random_string/;
 
-our $VERSION = '2.22.0';
+our $VERSION = '2.23.0';
 
 extends qw(
   Lemonldap::NG::Portal::Main::Issuer
@@ -218,7 +218,7 @@ sub load_config {
     my $config = {};
     my $res =
       $self->p->processHook( {}, 'getOidcRpConfig', $client_id, $config );
-    if ( $res != PE_OK ) {
+    if ( $res and $res != PE_OK ) {
         return;
     }
 
@@ -880,8 +880,13 @@ sub _authorizeEndpoint {
     # Create session_state
     my $session_state = $self->createSessionState( $req->id, $client_id );
 
-    # Check if PKCE is required
-    if ( $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsRequirePKCE}
+   # Check if PKCE is required
+   # RequirePKCE=1 (or any truthy value except 2): PKCE is mandatory
+   # RequirePKCE=2: PKCE or client_secret (checked at token endpoint)
+   # Backward compatibility: JSON::true or any truthy value != 2 is treated as 1
+    my $require_pkce =
+      $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsRequirePKCE};
+    if ( $require_pkce && $require_pkce != 2
         and !$oidc_request->{'code_challenge'} )
     {
         return $self->returnRedirectError(
@@ -977,19 +982,27 @@ sub _authorizeEndpoint {
             user => $req->sessionInfo->{ $self->conf->{whatToTrace} },
         );
 
+        my $response_params = {
+            ( $code  ? ( code  => $code )  : () ),
+            ( $state ? ( state => $state ) : () ),
+            (
+                $session_state
+                ? ( session_state => $session_state )
+                : ()
+            ),
+        };
+
+        $h = $self->p->processHook(
+            $req, 'oidcGenerateAuthorizationResponse',
+            $oidc_request, $rp, $response_params
+        );
+        return $h if ( $h != PE_OK );
+
         return $self->sendOidcResponse(
             $req, $flow,
             $oidc_request->{'response_mode'},
             $oidc_request->{'redirect_uri'},
-            {
-                ( $code  ? ( code  => $code )  : () ),
-                ( $state ? ( state => $state ) : () ),
-                (
-                    $session_state
-                    ? ( session_state => $session_state )
-                    : ()
-                ),
-            }
+            $response_params
         );
     }
 
@@ -1067,28 +1080,36 @@ sub _authorizeEndpoint {
             user => $req->sessionInfo->{ $self->conf->{whatToTrace} },
         );
 
+        my $response_params = {
+            id_token => $id_token,
+            (
+                $access_token
+                ? (
+                    token_type   => 'bearer',
+                    access_token => $access_token
+                  )
+                : ()
+            ),
+            ( $expires_in ? ( expires_in => $expires_in )    : () ),
+            ( $state      ? ( state      => $state )         : () ),
+            ( ( $req_scope ne $scope ) ? ( scope => $scope ) : () ),
+            (
+                $session_state ? ( session_state => $session_state )
+                : ()
+            )
+        };
+
+        my $h = $self->p->processHook(
+            $req, 'oidcGenerateAuthorizationResponse',
+            $oidc_request, $rp, $response_params
+        );
+        return $h if ( $h != PE_OK );
+
         return $self->sendOidcResponse(
             $req, $flow,
             $oidc_request->{'response_mode'},
             $oidc_request->{'redirect_uri'},
-            {
-                id_token => $id_token,
-                (
-                    $access_token
-                    ? (
-                        token_type   => 'bearer',
-                        access_token => $access_token
-                      )
-                    : ()
-                ),
-                ( $expires_in ? ( expires_in => $expires_in )    : () ),
-                ( $state      ? ( state      => $state )         : () ),
-                ( ( $req_scope ne $scope ) ? ( scope => $scope ) : () ),
-                (
-                    $session_state ? ( session_state => $session_state )
-                    : ()
-                )
-            }
+            $response_params
         );
     }
 
@@ -1199,32 +1220,40 @@ sub _authorizeEndpoint {
             user => $req->sessionInfo->{ $self->conf->{whatToTrace} },
         );
 
+        my $response_params = {
+            code => $code,
+            (
+                $access_token
+                ? (
+                    token_type   => 'bearer',
+                    access_token => $access_token
+                  )
+                : ()
+            ),
+            (
+                $id_token ? ( id_token => $id_token )
+                : ()
+            ),
+            ( $expires_in ? ( expires_in => $expires_in )    : () ),
+            ( $state      ? ( state      => $state )         : () ),
+            ( ( $req_scope ne $scope ) ? ( scope => $scope ) : () ),
+            (
+                $session_state ? ( session_state => $session_state )
+                : ()
+            )
+        };
+
+        my $h = $self->p->processHook(
+            $req, 'oidcGenerateAuthorizationResponse',
+            $oidc_request, $rp, $response_params
+        );
+        return $h if ( $h != PE_OK );
+
         return $self->sendOidcResponse(
             $req, $flow,
             $oidc_request->{'response_mode'},
             $oidc_request->{'redirect_uri'},
-            {
-                code => $code,
-                (
-                    $access_token
-                    ? (
-                        token_type   => 'bearer',
-                        access_token => $access_token
-                      )
-                    : ()
-                ),
-                (
-                    $id_token ? ( id_token => $id_token )
-                    : ()
-                ),
-                ( $expires_in ? ( expires_in => $expires_in )    : () ),
-                ( $state      ? ( state      => $state )         : () ),
-                ( ( $req_scope ne $scope ) ? ( scope => $scope ) : () ),
-                (
-                    $session_state ? ( session_state => $session_state )
-                    : ()
-                )
-            }
+            $response_params
         );
     }
 
@@ -1502,14 +1531,43 @@ sub token {
     $req->data->{dropCsp} = 1 if $self->conf->{oidcDropCspHeaders};
     $self->logger->debug("URL detected as an OpenID Connect TOKEN URL");
 
-    my ($rp) = $self->checkEndPointAuthenticationCredentials($req);
+    my ( $rp, $method ) = $self->checkEndPointAuthenticationCredentials($req);
     return $self->invalidClientResponse($req) unless ($rp);
 
     my $grant_type = $req->param('grant_type') || '';
 
+    # Hook to allow plugins to handle custom grant_types or modify parameters
+    # (e.g., CIBA, Device Authorization RFC 8628)
+    # PE_OK => continue processing
+    # PE_SENDRESPONSE => return custom response (for custom grant types)
+    my $h =
+      $self->p->processHook( $req, 'oidcGotTokenRequest', $rp, $grant_type );
+    if ( $h == PE_SENDRESPONSE ) {
+        return $req->response;
+    }
+
+    # A plugin may refuse this request
+    elsif ($h) {
+        if ( $h > 0 ) {
+            $self->auditLog(
+                $req,
+                code    => 'ISSUER_OIDC_BAD_REQUEST',
+                rp      => $rp,
+                message =>
+"An oidcGotTokenRequest plugin rejected this request with code $h",
+            );
+            return $self->sendOIDCError( $req, 'Bad request', 400 );
+        }
+        else {
+            $self->logger->error(
+                "An oidcGotTokenRequest plugin returned an usable code: $h");
+            return $self->sendOIDCError( $req, 'Server error', 500 );
+        }
+    }
+
     # Autorization Code grant
     if ( $grant_type eq 'authorization_code' ) {
-        return $self->_handleAuthorizationCodeGrant( $req, $rp );
+        return $self->_handleAuthorizationCodeGrant( $req, $rp, $method );
     }
 
     # Refresh token
@@ -1771,6 +1829,7 @@ sub _handlePasswordGrant {
                 user_session_id            => $req->id,
                 grant_type                 => "password",
                 $self->conf->{whatToTrace} => $user,
+                _oidc_sub => $self->getUserIDForRP( $req, $rp, $req->userData ),
             },
             0,
         );
@@ -1829,7 +1888,7 @@ sub _handlePasswordGrant {
 }
 
 sub _handleAuthorizationCodeGrant {
-    my ( $self, $req, $rp ) = @_;
+    my ( $self, $req, $rp, $method ) = @_;
     my $client_id = $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsClientID};
     my $code      = $req->param('code');
 
@@ -1846,7 +1905,29 @@ sub _handleAuthorizationCodeGrant {
 
     $codeSession->remove();
 
-    # Check PKCE
+    # Check PKCE requirement based on RP configuration
+    my $has_pkce   = $codeSession->data->{code_challenge} ? 1 : 0;
+    my $has_secret = ( $method && $method ne 'none' ) ? 1 : 0;
+    my $is_public = $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsPublic};
+    my $require_pkce =
+      $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsRequirePKCE} || 0;
+
+    # Check "PKCE or secret" mode (mode 2)
+    if ( $is_public and $require_pkce == 2 ) {
+        unless ( $has_pkce || $has_secret ) {
+            $self->userLogger->error(
+                "Relying Party $rp requires PKCE or valid secret");
+            return $self->invalidClientResponse($req);
+        }
+    }
+
+    # Check PKCE requirement (mode 1: always required)
+    if ( $require_pkce and $require_pkce != 2 and !$has_pkce ) {
+        $self->userLogger->error("Relying Party $rp requires PKCE");
+        return $self->invalidClientResponse($req);
+    }
+
+    # Validate PKCE challenge
     unless (
         $self->validatePKCEChallenge(
             $req->param('code_verifier'),
@@ -1957,7 +2038,6 @@ sub _handleAuthorizationCodeGrant {
     elsif ( $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsRefreshToken} ) {
         my $refreshTokenSession = $self->_generateRefreshToken(
             $req,
-
             $rp,
             {
                 redirect_uri => $codeSession->data->{redirect_uri},
@@ -1969,6 +2049,7 @@ sub _handleAuthorizationCodeGrant {
                 grant_type      => "authorizationcode",
                 _oidc_sid       => $sid,
                 $self->conf->{whatToTrace} => $user,
+                _oidc_sub => $self->getUserIDForRP( $req, $rp, $apacheSession->data ),
             },
             0,
         );
@@ -2025,7 +2106,8 @@ sub _handleAuthorizationCodeGrant {
     };
 
     $self->p->processHook( $req, 'oidcGenerateTokenResponse', $rp,
-        $token_response, $codeSession->data, $apacheSession->data );
+        $token_response, $codeSession->data, $apacheSession->data,
+        'authorization_code' );
 
     my %update;
 
@@ -2120,6 +2202,7 @@ sub _handleRefreshTokenGrant {
         my $h = $self->p->processHook( $req, 'oidcGotOnlineRefresh', $rp,
             $refreshSession->data, $userData );
         if ( $h != PE_OK ) {
+            return $req->response if $h == PE_SENDRESPONSE;
             return $self->sendOIDCError( $req, 'server_error', 500 );
         }
 
@@ -2172,6 +2255,7 @@ sub _handleRefreshTokenGrant {
         my $h = $self->p->processHook( $req, 'oidcGotOfflineRefresh', $rp,
             $refreshSession->data );
         if ( $h != PE_OK ) {
+            return $req->response if $h == PE_SENDRESPONSE;
             return $self->sendOIDCError( $req, 'server_error', 500 );
         }
 
@@ -2244,6 +2328,11 @@ sub _handleRefreshTokenGrant {
         ( $id_token ? ( id_token => "$id_token" ) : () ),
     };
 
+    # Allow plugins to modify token response (e.g., DPoP changing token_type)
+    my $h = $self->p->processHook( $req, 'oidcGenerateTokenResponse', $rp,
+        $token_response, $refreshSession->data, $userData, 'refresh_token' );
+    return $req->response if $h == PE_SENDRESPONSE;
+
     $self->logger->debug("Send token response");
 
     return $self->p->sendJSONresponse( $req, $token_response );
@@ -2305,7 +2394,7 @@ sub userInfo {
             code    => 'ISSUER_OIDC_USERINFO_FAILED',
             message => 'Missing access_token',
         );
-        return $self->returnBearerError( 'invalid_request',
+        return $self->returnBearerError( $req, 'invalid_request',
             "Access token not found in request", 401 );
     }
 
@@ -2319,7 +2408,7 @@ sub userInfo {
             code    => 'ISSUER_OIDC_USERINFO_FAILED',
             message => 'Invalid or expired access_token',
         );
-        return $self->returnBearerError( 'invalid_request',
+        return $self->returnBearerError( $req, 'invalid_request',
             'Invalid request', 401 );
     }
 
@@ -2341,7 +2430,7 @@ sub userInfo {
             code    => 'ISSUER_OIDC_USERINFO_FAILED',
             message => 'Missing required authentication header',
         );
-        return $self->returnBearerError( 'invalid_request',
+        return $self->returnBearerError( $req, 'invalid_request',
             'Invalid request', 400 );
     }
 
@@ -2358,13 +2447,14 @@ sub userInfo {
             code    => 'ISSUER_OIDC_USERINFO_FAILED',
             message => 'Unable to get session corresponding to access-token',
         );
-        return $self->returnBearerError( 'invalid_request',
+        return $self->returnBearerError( $req, 'invalid_request',
             'Invalid request', 401 );
     }
 
     my $userinfo_response =
       $self->buildUserInfoResponse( $req, $scope, $rp, $session );
-    return $self->returnBearerError( 'invalid_request', 'Invalid request', 401 )
+    return $self->returnBearerError( $req, 'invalid_request',
+        'Invalid request', 401 )
       unless ($userinfo_response);
 
     my $attr_str = join( ',', sort( keys(%$userinfo_response) ) );
@@ -2402,14 +2492,8 @@ sub userInfo {
               ->{oidcRPMetaDataOptionsUserInfoEncContentEncAlg},
         );
         $self->logger->debug("Return UserInfo as JWT: $userinfo_jwt");
-        return [
-            200,
-            [
-                'Content-Type'   => 'application/jwt',
-                'Content-Length' => length($userinfo_jwt)
-            ],
-            [$userinfo_jwt]
-        ];
+        return $self->p->sendBinaryResponse( $req, $userinfo_jwt,
+            type => 'application/jwt' );
     }
 }
 
@@ -2482,12 +2566,57 @@ sub introspection {
                   unless defined $response->{$_}
                   and $response->{$_} || length( $response->{$_} ) > 0;
             }
+
+            # Hook for plugins to modify introspection response
+            $self->p->processHook( $req, 'oidcGenerateIntrospectionResponse',
+                $response, $rp, $oidcSession->data );
         }
         else {
             $self->logger->warn("Count not find session tied to Access Token");
         }
     }
-    return $self->p->sendJSONresponse( $req, $response );
+
+    # RFC 9701: JWT Response for OAuth 2.0 Token Introspection
+    my $introspection_sign_alg =
+      $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsIntrospectionSignAlg};
+
+    unless ($introspection_sign_alg) {
+
+        # Default behavior: return JSON response (RFC 7662)
+        return $self->p->sendJSONresponse( $req, $response );
+    }
+
+    # RFC 9701: wrap response in token_introspection claim
+    my $jwt_payload = {
+        iss                 => $self->get_issuer($req),
+        aud                 => $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsClientID},
+        iat                 => time(),
+        token_introspection => $response,
+    };
+
+    my $introspection_jwt = $self->createJWT(
+        $jwt_payload,
+        $introspection_sign_alg,
+        $rp,
+        { typ => 'token-introspection+jwt' }
+    );
+
+    # Apply encryption if configured
+    my $enc_alg =
+      $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsIntrospectionEncKeyMgtAlg};
+    if ($enc_alg) {
+        my $enc_enc =
+          $self->rpOptions->{$rp}
+          ->{oidcRPMetaDataOptionsIntrospectionEncContentEncAlg}
+          || 'A256GCM';
+        $introspection_jwt =
+          $self->encryptToken( $rp, $introspection_jwt, $enc_alg, $enc_enc );
+    }
+
+    return $self->p->sendBinaryResponse(
+        $req, $introspection_jwt,
+        type => 'application/token-introspection+jwt'
+    );
 }
 
 sub unauthRevokeToken {
@@ -2594,7 +2723,7 @@ sub _revokeToken {
                 "$tokenHint dropped was " . $oidcSession->id );
             $oidcSession->remove;
         }
-        return [ 200, [], [] ];
+        return $self->p->sendBinaryResponse( $req, '' );
     }
     else {
         $self->auditLog(
@@ -2607,7 +2736,7 @@ sub _revokeToken {
             $tokenHint => $token
         );
     }
-    return [ 200, [], [] ];
+    return $self->p->sendBinaryResponse( $req, '' );
 }
 
 # Endpoint JWKS is implemented in Lib/OpenIDConnect
@@ -2695,6 +2824,12 @@ sub registration {
       $client_metadata->{id_token_encrypted_response_enc};
     my $userInfoEncAlg = $client_metadata->{userinfo_encrypted_response_alg};
     my $userInfoEncEnc = $client_metadata->{userinfo_encrypted_response_enc};
+    my $introspectionSignAlg =
+      $client_metadata->{introspection_signed_response_alg};
+    my $introspectionEncAlg =
+      $client_metadata->{introspection_encrypted_response_alg};
+    my $introspectionEncEnc =
+      $client_metadata->{introspection_encrypted_response_enc};
 
     # Register RP in global configuration
     my $conf = $self->confAcc->getConf( { raw => 1, noCache => 1 } );
@@ -2756,6 +2891,16 @@ sub registration {
     $conf->{oidcRPMetaDataOptions}->{$rp}
       ->{oidcRPMetaDataOptionsUserInfoEncContentEncAlg} = $userInfoEncEnc
       if $userInfoEncEnc;
+    $conf->{oidcRPMetaDataOptions}->{$rp}
+      ->{oidcRPMetaDataOptionsIntrospectionSignAlg} = $introspectionSignAlg
+      if $introspectionSignAlg;
+    $conf->{oidcRPMetaDataOptions}->{$rp}
+      ->{oidcRPMetaDataOptionsIntrospectionEncKeyMgtAlg} = $introspectionEncAlg
+      if $introspectionEncAlg;
+    $conf->{oidcRPMetaDataOptions}->{$rp}
+      ->{oidcRPMetaDataOptionsIntrospectionEncContentEncAlg} =
+      $introspectionEncEnc
+      if $introspectionEncEnc;
 
     # TODO "jwks" support (when jwks_uri isn't available
 
@@ -2794,6 +2939,15 @@ sub registration {
         $registration_response->{'userinfo_signed_response_alg'} =
           $userinfo_signed_response_alg
           if defined $userinfo_signed_response_alg;
+        $registration_response->{'introspection_signed_response_alg'} =
+          $introspectionSignAlg
+          if defined $introspectionSignAlg;
+        $registration_response->{'introspection_encrypted_response_alg'} =
+          $introspectionEncAlg
+          if defined $introspectionEncAlg;
+        $registration_response->{'introspection_encrypted_response_enc'} =
+          $introspectionEncEnc
+          if defined $introspectionEncEnc;
     }
     else {
         $self->logger->error(
@@ -2856,7 +3010,7 @@ sub endSessionDone {
           $self->buildLogoutResponse( $post_logout_redirect_uri, $state );
 
         $self->logger->debug("Redirect user to $response_url");
-        return [ 302, [ Location => $response_url ], [] ];
+        return $self->p->sendRedirection( $req, $response_url, code => 302 );
     }
 
     # Else, normal login process
@@ -3008,8 +3162,12 @@ sub metadata {
     my %args;
     $args{ttl} = $self->conf->{oidcServiceMetadataTtl}
       if $self->conf->{oidcServiceMetadataTtl};
-    return $self->p->sendJSONresponse( $req,
-        $self->metadataDoc( $self->get_issuer($req) ), %args );
+    my $metadata = $self->metadataDoc( $self->get_issuer($req) );
+
+    # Hook for plugins to add metadata fields (e.g., JARM, CIBA, PAR, DPoP)
+    $self->p->processHook( $req, 'oidcGenerateMetadata', $metadata );
+
+    return $self->p->sendJSONresponse( $req, $metadata, %args );
 }
 
 # Store request parameters in %ENV
@@ -3162,6 +3320,10 @@ sub _convertOldFormatConsents {
 sub _generateRefreshToken {
     my ( $self, $req, $rp, $info, $offline ) = @_;
 
+    # Allow plugins to add data to refresh token
+    my $h = $self->p->processHook( $req, 'oidcGenerateRefreshToken', $info, $rp, $offline );
+    return if $h;
+
     my $user  = $info->{_session_uid};
     my $token = $self->newRefreshToken( $rp, $info, $offline );
     if ($token) {
--- a/usr/share/perl5/Lemonldap/NG/Portal/Lib/OpenIDConnect.pm
+++ b/usr/share/perl5/Lemonldap/NG/Portal/Lib/OpenIDConnect.pm
@@ -5,7 +5,9 @@
 # Common OpenID Connect functions
 package Lemonldap::NG::Portal::Lib::OpenIDConnect;
 
+use Mouse;
 use strict;
+use Crypt::URandom;
 use Crypt::OpenSSL::RSA;
 use Crypt::OpenSSL::X509;
 use Crypt::JWT  qw(encode_jwt decode_jwt);
@@ -20,16 +22,13 @@ use MIME::Base64
   qw/encode_base64 decode_base64 encode_base64url decode_base64url/;
 use Scalar::Util qw/looks_like_number/;
 use URI;
-use URI::QueryParam;
-use Mouse;
-use Crypt::URandom;
-use URI;
 use URI::Escape qw(uri_unescape);
+use URI::QueryParam;
 
 use Lemonldap::NG::Portal::Main::Constants
   qw(PE_OK PE_REDIRECT PE_ERROR portalConsts);
 
-our $VERSION = '2.22.2';
+our $VERSION = '2.23.0';
 
 use constant oidcErrorLevel => {
     server_error     => 'error',
@@ -82,6 +81,24 @@ has state_ott => (
     }
 );
 
+# Calculate conditional plugins to be loaded if required by a dynamic RP
+has _rpConditionalPlugins => (
+    is      => 'ro',
+    lazy    => 1,
+    default => sub {
+        my %map;
+        for ( my $i = 0 ; $i < @Lemonldap::NG::Portal::Main::pList ; $i += 2 )
+        {
+            if ( $Lemonldap::NG::Portal::Main::pList[$i] =~
+                m{^or::oidcRPMetaDataOptions/\*/(.+)$} )
+            {
+                $map{$1} = $Lemonldap::NG::Portal::Main::pList[ $i + 1 ];
+            }
+        }
+        return \%map;
+    }
+);
+
 # METHODS
 
 # Load OpenID Connect Providers and JWKS data
@@ -151,6 +168,12 @@ sub load_rp {
     my ( $self, %config ) = @_;
     my $rp = $config{confKey};
 
+    # Check if RP is activated
+    unless ( $config{options}->{oidcRPMetaDataOptionsActivation} // 1 ) {
+        $self->logger->debug("Processing $rp: RP Deactivated");
+        return 0;
+    }
+
     my $valid = 1;
 
     # Handle scopes
@@ -231,7 +254,7 @@ sub load_rp {
             or $config{options}->{oidcRPMetaDataOptionsJwks} )
       )
     {
-        $self->logger->debug("Processing JWKS options for RP $rp...");
+        $self->logger->debug("Processing JWKS options for RP $rp");
         my $jwks = $config{options}->{oidcRPMetaDataOptionsJwks};
         $jwks = $self->decodeJSON($jwks) if $jwks and not ref $jwks;
 
@@ -314,6 +337,10 @@ sub load_rp {
         $self->rpScopeRules->{$rp} = $compiled_scope_rules;
         $self->rpRules->{$rp}      = $rule;
         $self->rpLevelRules->{$rp} = $levelrule;
+
+        # Ensure plugins required by this RP's options are loaded (#3587)
+        $self->_loadConditionalPlugins( $config{options} );
+
         return 1;
     }
     else {
@@ -324,6 +351,23 @@ sub load_rp {
     return 0;
 }
 
+# Plugins that must be loaded when specific RP options are enabled (#3587)
+# Built from @Lemonldap::NG::Portal::Main::pList entries matching
+# or::oidcRPMetaDataOptions/*/...
+sub _loadConditionalPlugins {
+    my ( $self, $options ) = @_;
+
+    while ( my ( $opt, $plugin ) = each %{ $self->_rpConditionalPlugins } ) {
+        next unless $options->{$opt};
+        my $full =
+          ( $plugin =~ /^::/ ) ? "Lemonldap::NG::Portal$plugin" : $plugin;
+        next if $self->p->loadedModules->{$full};
+        $self->logger->info(
+            "Dynamically loading plugin $plugin for RP option $opt");
+        $self->p->loadPlugin($plugin);
+    }
+}
+
 # Refresh JWKS data if needed
 # @param no_cache Disable cache update
 # @return boolean result
@@ -407,7 +451,6 @@ sub getCallbackUri {
     my ( $self, $req ) = @_;
 
     my $callback_get_param = $self->conf->{oidcRPCallbackGetParam};
-
     my $callback_uri =
       $self->p->buildUrl( $req->portal, { $callback_get_param => 1 } );
 
@@ -542,6 +585,9 @@ sub sendOidcResponse {
     my ( $self, $req, $flow, $response_mode, $redirect_uri, $response_params )
       = @_;
 
+    # RFC 9207: Add iss parameter to prevent mix-up attacks
+    $response_params->{iss} = $self->get_issuer($req);
+
     $response_mode //= $self->getDefaultResponseModeForFlow($flow);
 
     if ( $response_mode eq "query" ) {
@@ -1039,9 +1085,8 @@ sub refreshAccessToken {
     my ( $self, $req, $op, $session ) = @_;
 
     # Handle unauthenticated OIDC calls
-    my $data       = $session ? $session->data : $req->userData;
-    my $session_id = $session ? $session->id   : $req->id;
-
+    my $data          = $session ? $session->data : $req->userData;
+    my $session_id    = $session ? $session->id   : $req->id;
     my $refresh_token = $data->{_oidc_refresh_token};
 
     if ($refresh_token) {
@@ -1114,15 +1159,15 @@ sub getUserInfo {
     }
 
     $self->logger->debug(
-        "Request User Info on $userinfo_uri with access token $access_token");
+        "Request UserInfo on $userinfo_uri with access token $access_token");
 
     my $response = $self->ua->get( $userinfo_uri,
         "Authorization" => "Bearer $access_token" );
 
     if ( $response->is_error ) {
-        $self->logger->error( "Bad userinfo response: " . $response->message );
+        $self->logger->error( "Bad UserInfo response: " . $response->message );
         $self->logger->debug(
-            "Userinfo response received: " . $response->as_string );
+            "UserInfo response received: " . $response->as_string );
         return 0;
     }
 
@@ -1138,6 +1183,12 @@ sub getUserInfo {
         my $jwt = $self->decryptJwt($userinfo_content);
         return $self->decodeJWT( $jwt, $op );
     }
+    else {
+        $self->logger->error('Bad UserInfo Content-Type');
+        $self->logger->debug( "UserInfo response received with"
+              . " invalid Content-Type ($content_type)" );
+        return 0;
+    }
 }
 
 # Convert JSON to HashRef
@@ -1205,19 +1256,16 @@ sub getAuthorizationCode {
 
 sub newAccessToken {
     my ( $self, $req, $rp, $scope, $sessionInfo, $info ) = @_;
-
     my $client_id = $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsClientID};
-
-    my $at_info = {
-
+    my $at_info   = {
         scope     => $scope,
         rp        => $rp,
         client_id => $client_id,
         iat       => time,
         nbf       => time,
+        aud       => $self->getAudiences($rp),
         %{$info},
     };
-
     my $ttl =
          $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsAccessTokenExpiration}
       || $self->conf->{oidcServiceAccessTokenExpiration};
@@ -1278,7 +1326,7 @@ sub makeJWT {
 
     my $access_token_payload = {
         iss => $self->get_issuer($req),     # Issuer Identifier
-        exp => $exp,                        # expiration
+        exp => $exp,                        # Expiration
         aud => $self->getAudiences($rp),    # Audience
         jti => $id,                         # Access Token session ID
         sid => $self->getSidFromSession( $rp, $sessionInfo ),    # Session id
@@ -1425,7 +1473,7 @@ sub updateToken {
 
     if ( $oidcSession->error ) {
         $self->userLogger->warn(
-            "OpenIDConnect session $id isn't yet available");
+            "OpenIDConnect session $id not found");
         return undef;
     }
 
@@ -1440,6 +1488,8 @@ sub getOpenIDConnectSession {
     my $id   = shift;
     my $type = shift;
 
+    my $type_log = $type || "session";
+
     # Check old method signature ($id, $type, $ttl, $info)
     my %opts =
         ( ( $_[0] and $_[0] =~ /^\d+$/ ) or ( $_[1] and ref $_[1] ) )
@@ -1478,10 +1528,10 @@ sub getOpenIDConnectSession {
     if ( $oidcSession->error ) {
         if ($id) {
             $self->userLogger->warn(
-                "OpenIDConnect session $id isn't yet available");
+                "OpenIDConnect $type_log $id not found");
         }
         else {
-            $self->logger->error("Unable to create new OpenIDConnect session");
+            $self->logger->error("Unable to create new OpenIDConnect $type_log");
             $self->logger->error( $oidcSession->error );
         }
         return undef;
@@ -1504,7 +1554,7 @@ sub getOpenIDConnectSession {
         if (
             time > ( $oidcSession->{data}->{_utime} + $self->conf->{timeout} ) )
         {
-            $self->logger->notice("Session $id has expired");
+            $self->logger->notice("OpenIDConnect $type_log $id has expired");
             return undef;
         }
     }
@@ -1512,7 +1562,7 @@ sub getOpenIDConnectSession {
     # Make sure the token is still valid, we already compensated for
     # different TTLs when storing _utime
     if ( time > ( $oidcSession->{data}->{_utime} + $self->conf->{timeout} ) ) {
-        $self->logger->notice("Session $id has expired");
+        $self->logger->notice("OpenIDConnect $type_log $id has expired");
         return undef;
     }
 
@@ -1647,7 +1697,7 @@ sub decodeJWT {
                 "Cannot verify $alg signature: no JWKS data found");
             return;
         }
-        unless ($jwks->{keys}
+        unless ( $jwks->{keys}
             and ref( $jwks->{keys} ) eq 'ARRAY'
             and @{ $jwks->{keys} } )
         {
@@ -1864,16 +1914,16 @@ sub sendOIDCError {
 # @param error_message Error message
 # @return GI response
 sub returnBearerError {
-    my ( $self, $error_code, $error_message ) = @_;
+    my ( $self, $req, $error_code, $error_message ) = @_;
 
-    my $res = [
-        401,
-        [
+    my $res = $self->p->sendBinaryResponse(
+        $req, '',
+        code    => 401,
+        headers => [
             'WWW-Authenticate' =>
               "error=$error_code,error_description=$error_message"
-        ],
-        []
-    ];
+        ]
+    );
 
     $self->p->setCorsHeaderFromConfig($res);
 
@@ -1982,7 +2032,7 @@ sub getEndPointAuthenticationCredentials {
 
         # Unescape clientId and clientSecret to be compliant with RFC 6749
         # https://datatracker.ietf.org/doc/html/rfc6749
-        $client_id = uri_unescape($client_id);
+        $client_id     = uri_unescape($client_id);
         $client_secret = uri_unescape($client_secret);
 
         # Using multiple methods is an error
@@ -2796,6 +2846,16 @@ sub validatePKCEChallenge {
     my ( $self, $code_verifier, $code_challenge, $code_challenge_method ) = @_;
 
     unless ($code_challenge) {
+
+        # RFC 9700 section 2.1.1: Mitigate PKCE downgrade attacks
+        # Reject code_verifier if no code_challenge was sent during authorization
+        if ($code_verifier) {
+            $self->userLogger->error(
+                "PKCE downgrade attack: code_verifier provided "
+              . "but no code_challenge was sent during authorization"
+            );
+            return 0;
+        }
         $self->logger->debug("PKCE was not requested by the RP");
         return 1;
     }
@@ -2868,6 +2928,9 @@ sub getAudiences {
 sub getUserIDForRP {
     my ( $self, $req, $rp, $data ) = @_;
 
+    # Use pre-computed sub if available (stored in online refresh tokens)
+    return $data->{_oidc_sub} if $data->{_oidc_sub};
+
     my $user_id_attribute =
          $self->rpOptions->{$rp}->{oidcRPMetaDataOptionsUserIDAttr}
       || $self->conf->{whatToTrace};
--- a/usr/share/perl5/Lemonldap/NG/Portal/UserDB/OpenIDConnect.pm
+++ b/usr/share/perl5/Lemonldap/NG/Portal/UserDB/OpenIDConnect.pm
@@ -10,7 +10,7 @@ use Lemonldap::NG::Portal::Main::Constants qw(
   PE_OK
 );
 
-our $VERSION = '2.21.0';
+our $VERSION = '2.23.0';
 
 extends qw(
   Lemonldap::NG::Portal::Main::UserDB
@@ -113,10 +113,17 @@ sub setSessionInfo {
 
     while ( my ( $k, $v ) = each %vars ) {
         my $value = $req->data->{OpenIDConnect_user_info}->{$v};
-        if ( ref($value) and ref($value) eq "ARRAY" ) {
+
+        # Flatten arrays if all their values are scalars
+        if (    ref($value)
+            and ref($value) eq "ARRAY"
+            and not grep { ref($_) } @$value )
+        {
             $req->{sessionInfo}->{$k} =
               join( $self->conf->{multiValuesSeparator}, @$value );
         }
+
+        # Else, store the value as-is
         else {
             $req->{sessionInfo}->{$k} = $value;
         }
